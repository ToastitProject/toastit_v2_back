package org.toastit_v2.common.common.application.type;

/**
 * 정렬 방식에 대한 열거형(Enum).
 * 최신순, 오래된 순, 조회수 순, 좋아요 순으로 정렬할 때 사용됨.
 */
public enum SortType {

    NEWEST,
    OLDEST,
    VIEWCOUNT,
    LIKECOUNT

}
